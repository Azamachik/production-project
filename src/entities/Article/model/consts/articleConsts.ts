export enum ArticleBlockType {
    CODE = 'CODE',
    TEXT = 'TEXT',
    IMAGE = 'IMAGE',
    NOTE = 'NOTE',
    QUOTE = 'QUOTE',
}

export enum ArticleView {
    GRID = 'GRID',
    LIST = 'LIST',
}
